#include <stdio.h>

int main () {
    int number = 12;
    char name[] = "daniel";
    float decimal = 12.32;

    printf("number %d", number);
    printf("name %s", name);
    printf("decimal %.2f", decimal);

    return 0;
}